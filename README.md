# Email-spam-deatction-
This project is a Python-based machine learning application that detects whether an email is spam or ham (legitimate). It uses text preprocessing, feature extraction with TF-IDF, and a classification model such as Logistic Regression or Naive Bayes to analyze email content and predict spam messages accurately
